import React from 'react';
import styles from './AddColumnButton.module.scss';

export function AddColumnButton(props: any) {
  return (
    <button className={styles.button}>
      +
    </button>
  );
}