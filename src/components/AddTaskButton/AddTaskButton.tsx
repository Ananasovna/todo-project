import React from 'react';
import styles from './AddTaskButton.module.scss';

export function AddTaskButton(props: any) {
  return (
    <button className={styles.button}>
      +
    </button>
  );
}